# __APP_ID__

Copy of `apps/_template/` - describe what this app does here.

## Run

```sh
podman-compose -f apps/__APP_ID__/docker-compose.yml up -d
```

Then visit `http://catasophie.local__URL_PATH__`.

## Data

Persistent data lives in `./data/` (gitignored).
